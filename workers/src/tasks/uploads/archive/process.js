const { InputOutput } = require('../../../io');

/**
 * @param {InputOutput} io
 * @param {any} upload
 */
function processArchive(io, upload) {
    // Unzip and dispatch
}

module.exports = { processArchive };
